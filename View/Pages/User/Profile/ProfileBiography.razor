<div @attributes="@InputAttributes">

    @if (Editable)
    {
        <span class="editable d-inline-block" @onclick="@(() => dialog=true)" OnClickStopPropagation>@Value</span>

        <MDialog @bind-Value="dialog" MaxWidth="500" Width="320" OnOutsideClick="Close">
            <ChildContent>
                <MCard Tile Loading="loading.Value">
                    <ProgressContent>
                        <MProgressLinear Height="10" Indeterminate />
                    </ProgressContent>
                    <ChildContent>
                        <MForm Disabled="loading" Model="command" EnableI18n OnValidSubmit="Submit" ValidateOn="ValidateOn.Blur">
                            <MCardTitle>
                                <MI18n>Biography</MI18n>
                            </MCardTitle>
                            <MCardText>
                                <MTextarea Required Autofocus Clearable Counter="50" maxlength="50"
                                            Loading="loading.Value"
                                            Label="@I18n.T("Biography")"
                                            @bind-Value="command.Biography" />
                            </MCardText>
                            <MCardActions>
                                <MSpacer></MSpacer>
                                <MButton Color="blue darken-1" Text OnClick="Close">
                                    Close
                                </MButton>
                                <MButton Color="green darken-1" Text Type="submit" Disabled="loading">
                                    Save
                                </MButton>
                            </MCardActions>
                        </MForm>
                    </ChildContent>
                </MCard>
            </ChildContent>
        </MDialog>
    }
    else
    {
        <span>@Value</span>
    }

</div>


<style>
    .editable {
        transition: all 0.3s;
        cursor: pointer;
    }

        .editable:hover {
            transform: scale(1.02)
        }
</style>